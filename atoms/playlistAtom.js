
import { atom } from "recoil";

export const playlistsState = atom({key: 'playlistsState',default: []})
export const playlistIdState = atom({key: 'playlistIdState',default: ''})
export const playlistsByIdState = atom({key: 'playlistsByIdState',default: []})